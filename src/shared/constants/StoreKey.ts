export enum StoreKey {
  OpenInNonPrivateEnabled = 'openInNonPrivateEnabled',
  BookmarkFolder = 'bookmarksFolder',
  Addresses = 'addresses',
  Evacuation = 'evacuation',
  Action = 'action',
  Options = 'options',
  IncognitoEnabled = 'incognitoEnabled',
  PanicModeEnabled = 'panicModeEnabled',
}
